import 'package:flutter/material.dart';

class ThemeColors {
  static Color mainBGColor = Colors.deepPurple[100];
  static Color numButtonColor = Colors.white;
  static Color clearButtonColor = Colors.green;
  static Color deleteButtonColor = Colors.redAccent;
  static Color numButtonTextColor = Colors.deepPurple;
  static Color oprationButtonColor = Colors.deepPurple;
  static Color oprationButtonTextColor = Colors.white;
}
